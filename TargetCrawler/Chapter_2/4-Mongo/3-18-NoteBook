比较符号:

符号          含义          示例
$lt         <      {'查询对象(age)':{'$lt':''}(查询条件)}
$gt         >
$lte        <=
$gte        =>
$ne         < >
$in        在范围内     {'查询对象(age)':{'$in':[20,23]}}
$nin       不在范围内     {'查询对象(age)':{'$nin':[20,23]}}

